#:schema https://mise.jdx.dev/schema/mise.json

[tools]

# core tools
go = "latest"
node = "latest"
lua = "latest"
ruby = "latest"
rust = { version = "latest", components = "rust-analyzer,clippy" }
python = "latest"
uv = "latest"

# software verification (see https://mise.jdx.dev/tips-and-tricks.html#software-verification)
slsa-verifier = "latest"
cosign = "latest"

# provides completion and parsing support for tasks see:
# https://mise.jdx.dev/tasks/file-tasks.html#arguments
usage = "latest"

# FIXME: mise hangs frequently when trying to install from npm
# see <https://github.com/jdx/mise/issues/4302>
# see <https://github.com/jdx/mise/discussions/4275>
#
# "npm:mcp-hub" = "latest"
# "npm:prettier" = "latest"

[settings]
experimental = true
python.uv_venv_auto = true
pipx.uvx = true

